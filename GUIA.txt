╔══════════════════════════════════════════════════════════════════════════════╗
║                   GUÍA DE CONFIGURACIÓN - PASO A PASO                         ║
╚══════════════════════════════════════════════════════════════════════════════╝

SECCIÓN 1: VERIFICAR QUE LA APLICACIÓN ESTÁ FUNCIONANDO
════════════════════════════════════════════════════════════════════════════════

✅ La aplicación Electron ya está corriendo en background

Pasos:
1. Abre la aplicación (debe estar en una ventana)
2. Haz clic en "Permitir" para acceder al micrófono
3. Di "hola" (la palabra de activación por defecto)
4. Observa que la barra de búsqueda se ilumina en verde 🟢

Si todo funciona correctamente, verás:
• El botón del micrófono se vuelve verde
• La barra parpadea con resplandor verde
• Escucharás un sonido "beep" de confirmación


SECCIÓN 2: CAMBIAR LA PALABRA DE ACTIVACIÓN
════════════════════════════════════════════════════════════════════════════════

OPCIÓN A: Desde la interfaz (Recomendado - Sin reiniciar)
──────────────────────────────────────────────────────────
1. En la primera barra, escribe: "hey" (u otra palabra)
2. Haz clic en "Actualizar"
3. Verás un mensaje confirmando el cambio
4. Ahora escucha la nueva palabra

OPCIÓN B: Desde el archivo .env (Requiere reinicio)
─────────────────────────────────────────────────────
1. Abre el archivo .env
2. Cambia: ACTIVATION_WORD=hey
3. Guarda y reinicia la aplicación


SECCIÓN 3: INTEGRAR TU API DE TRANSCRIPCIÓN
════════════════════════════════════════════════════════════════════════════════

Cuando recibas los datos de tu API, sigue estos pasos:

PASO 1: Copiar la función correspondiente
──────────────────────────────────────────
• Abre api-examples.js
• Busca tu API (Google, Azure, Whisper, etc.)
• Copia la función completa

PASO 2: Actualizar las credenciales
─────────────────────────────────────
Ejemplo para Google:

const GOOGLE_STT_CONFIG = {
  API_KEY: 'sk-...tu-api-key-aqui...',
  LANGUAGE_CODE: 'es-ES',
};

PASO 3: Modificar renderer.js
──────────────────────────────
Reemplaza esta función:

  function activateMode() {
    mainSearchBar.classList.add('active');
    // ... código actual
  }

Con esta:

  async function activateMode() {
    const mainSearchBar = document.getElementById('mainSearchBar');
    const searchInput = document.getElementById('searchInput');
    
    mainSearchBar.classList.add('active');
    
    try {
      // Grabar 5 segundos de audio
      const audioBlob = await recordAudio(5000);
      console.log('Audio grabado');
      
      // Llamar tu API (ejemplo con Google)
      const transcription = await transcribeWithGoogle(audioBlob);
      
      // Llenar la búsqueda y ejecutar
      searchInput.value = transcription;
      document.getElementById('searchBtn').click();
      
    } catch (error) {
      console.error('Error:', error);
    } finally {
      setTimeout(() => {
        mainSearchBar.classList.remove('active');
      }, 3000);
    }
  }

PASO 4: Agregar función para grabar audio
───────────────────────────────────────────
Copia esta función al final de renderer.js:

  async function recordAudio(duration = 5000) {
    return new Promise(async (resolve, reject) => {
      try {
        const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
        const mediaRecorder = new MediaRecorder(stream);
        const audioChunks = [];
        
        mediaRecorder.ondataavailable = (event) => {
          audioChunks.push(event.data);
        };
        
        mediaRecorder.onstop = () => {
          const audioBlob = new Blob(audioChunks, { type: 'audio/webm' });
          stream.getTracks().forEach(track => track.stop());
          resolve(audioBlob);
        };
        
        mediaRecorder.start();
        
        setTimeout(() => {
          mediaRecorder.stop();
        }, duration);
        
      } catch (error) {
        reject(error);
      }
    });
  }

PASO 5: Guardar y probar
────────────────────────
1. Guarda los cambios
2. Reinicia la aplicación (Ctrl+C en terminal, luego npm start)
3. Di la palabra de activación
4. Habla durante 5 segundos
5. Verifica la transcripción


SECCIÓN 4: EJEMPLOS ESPECÍFICOS POR API
════════════════════════════════════════════════════════════════════════════════

GOOGLE CLOUD SPEECH-TO-TEXT
────────────────────────────

1. Credenciales necesarias:
   • API_KEY de Google Cloud
   • Habilitar Speech-to-Text API

2. En renderer.js, agregar al inicio:

   const GOOGLE_STT_CONFIG = {
     API_KEY: 'AIza...tu-clave-aqui...',
     LANGUAGE_CODE: 'es-ES'
   };

   async function transcribeWithGoogle(audioBlob) {
     const base64 = await blobToBase64(audioBlob);
     const response = await fetch(
       `https://speech.googleapis.com/v1/speech:recognize?key=${GOOGLE_STT_CONFIG.API_KEY}`,
       {
         method: 'POST',
         body: JSON.stringify({
           config: {
             encoding: 'WEBM_OPUS',
             languageCode: 'es-ES'
           },
           audio: { content: base64 }
         })
       }
     );
     const data = await response.json();
     return data.results?.[0]?.alternatives?.[0]?.transcript || '';
   }

   function blobToBase64(blob) {
     return new Promise((resolve) => {
       const reader = new FileReader();
       reader.onloadend = () => resolve(reader.result.split(',')[1]);
       reader.readAsDataURL(blob);
     });
   }

AZURE SPEECH-TO-TEXT
─────────────────────

1. Credenciales necesarias:
   • API_KEY de Azure
   • REGION (ej: eastus)

2. En renderer.js:

   const AZURE_CONFIG = {
     API_KEY: 'tu-key-aqui',
     REGION: 'eastus'
   };

   async function transcribeWithAzure(audioBlob) {
     const response = await fetch(
       `https://${AZURE_CONFIG.REGION}.tts.speech.microsoft.com/cognitiveservices/v1`,
       {
         method: 'POST',
         body: audioBlob,
         headers: {
           'Ocp-Apim-Subscription-Key': AZURE_CONFIG.API_KEY,
           'Content-Type': 'audio/wav'
         }
       }
     );
     const data = await response.json();
     return data.DisplayText || '';
   }

OPENAI WHISPER
───────────────

1. Credenciales necesarias:
   • API_KEY de OpenAI
   • Suscripción activa

2. En renderer.js:

   async function transcribeWithWhisper(audioBlob) {
     const formData = new FormData();
     formData.append('file', audioBlob, 'audio.webm');
     formData.append('model', 'whisper-1');
     
     const response = await fetch('https://api.openai.com/v1/audio/transcriptions', {
       method: 'POST',
       headers: { 'Authorization': 'Bearer sk-...' },
       body: formData
     });
     
     const data = await response.json();
     return data.text || '';
   }


SECCIÓN 5: SOLUCIÓN DE PROBLEMAS
════════════════════════════════════════════════════════════════════════════════

PROBLEMA: "No se escucha nada"
──────────────────────────────
Soluciones:
1. Verifica que permitiste el acceso al micrófono
2. Comprueba que tu micrófono está conectado
3. Abre la consola: F12 > Console
4. Deberías ver "Reconocimiento de voz iniciado"

PROBLEMA: "La palabra no se detecta"
─────────────────────────────────────
Soluciones:
1. Pronuncia más claramente
2. Reduce el ruido de fondo
3. Usa palabras cortas (ej: "hola", "hey")
4. Haz clic en "Actualizar" después de cambiar
5. Abre F12 > Console para ver lo que escucha

PROBLEMA: "Error de API al transcribir"
───────────────────────────────────────
Soluciones:
1. Verifica que el API_KEY es correcto
2. Comprueba que tu suscripción está activa
3. Revisa los logs: F12 > Console
4. Intenta con un archivo de audio de prueba

PROBLEMA: "No puedo actualizar la palabra"
────────────────────────────────────────────
Soluciones:
1. Asegúrate de escribir la palabra
2. Haz clic en "Actualizar" o presiona Enter
3. Reinicia la aplicación si no funciona

PROBLEMA: "Aplicación muy lenta"
─────────────────────────────────
Soluciones:
1. Cierra otras aplicaciones
2. Reduce el tiempo de grabación (de 5s a 3s)
3. Usa un micrófono mejor
4. Revisa si hay mucho ruido de fondo


SECCIÓN 6: VERIFICAR QUE TODO FUNCIONA
════════════════════════════════════════════════════════════════════════════════

Abre la consola del navegador: F12 > Console

Deberías ver mensajes como:

✓ "Reconocimiento de voz iniciado"
✓ "Escuchado (final): hola"
✓ "¡PALABRA DE ACTIVACIÓN DETECTADA!: hola"
✓ "¡Palabra de activación detectada - Iniciando grabación de audio..."

Si ves errores:
1. Copia el error completo
2. Revisa en Google el mensaje de error
3. Verifica las credenciales de tu API


SECCIÓN 7: ESTRUCTURA FINAL DE ARCHIVOS
════════════════════════════════════════════════════════════════════════════════

youuuu/
├── index.html          # Interfaz (NO MODIFICAR)
├── renderer.js         # Lógica (MODIFICAR AQUÍ para integración)
├── main.js             # Electron (NO MODIFICAR)
├── preload.js          # Seguridad (NO MODIFICAR)
├── .env                # Config (Guardar credenciales aquí)
├── api-examples.js     # Ejemplos de APIs (REFERENCE)
├── api-integration.js  # Template (REFERENCE)
├── IMPLEMENTACION.txt  # Resumen (INFO)
├── GUIA.txt           # Este archivo (INFO)
└── package.json        # Dependencias (NO MODIFICAR)


SECCIÓN 8: COMANDO ÚTILES
════════════════════════════════════════════════════════════════════════════════

# Iniciar la aplicación
npm start

# Abrir consola de desarrollador (dentro de la app)
Ctrl + Shift + I

# Recargar la aplicación
Ctrl + R

# Detener la aplicación
Ctrl + C (en la terminal)

# Ver logs en tiempo real
Ctrl + Shift + I > Console


SECCIÓN 9: CHECKLIST FINAL
════════════════════════════════════════════════════════════════════════════════

Antes de empezar con la API, verifica:

☐ La aplicación está corriendo (npm start)
☐ El botón de micrófono funciona y pide permiso
☐ Se escucha la palabra de activación
☐ La barra se ilumina en verde cuando escucha
☐ Puedes cambiar la palabra desde la interfaz
☐ YouTube carga correctamente
☐ Búsqueda manual en YouTube funciona

Una vez integrada la API:

☐ Credenciales de API configuradas correctamente
☐ Función de transcripción agregada a renderer.js
☐ Función recordAudio() copiada
☐ activateMode() actualizada
☐ Archivos guardados y aplicación reiniciada
☐ Prueba: decir palabra clave > hablar > transcripción automática

════════════════════════════════════════════════════════════════════════════════

¿DUDAS O PROBLEMAS?

1. Revisa la consola: F12 > Console
2. Lee los logs que aparecen ahí
3. Compara con los ejemplos en api-examples.js
4. Verifica credenciales de API
5. Intenta con una API de prueba primero

¡LISTO! Tu aplicación está 100% lista para integrar cualquier API de speech-to-text.

════════════════════════════════════════════════════════════════════════════════
